{
  "name": "asclepius-clinical-reference",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate:hjh": "node scripts/generate-hjh-official-corrections.mjs",
    "generate:cmjah": "node scripts/generate-cmjah-official-corrections.mjs",
    "generate:clinical": "npm run generate:hjh && npm run generate:cmjah",
    "dev": "npm run generate:clinical && node scripts/generate-pwa-assets.js && vite --port=3000 --host=0.0.0.0",
    "build": "npm run generate:clinical && node scripts/generate-pwa-assets.js && vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "validate:data": "npm run generate:clinical && node --import tsx scripts/validate-clinical-data.ts",
    "test": "npm run generate:clinical && vitest run",
    "test:watch": "npm run generate:clinical && vitest",
    "check": "npm run typecheck && npm run validate:data && npm run test && npm run build"
  },
  "dependencies": {
    "lucide-react": "^0.546.0",
    "motion": "^12.23.24",
    "react": "^19.0.1",
    "react-dom": "^19.0.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.14",
    "@types/node": "^22.14.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^5.0.4",
    "tailwindcss": "^4.1.14",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.3",
    "vitest": "^3.2.4"
  }
}
